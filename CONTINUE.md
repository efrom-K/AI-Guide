# CONTINUE.md — состояние проекта «AI Audio Guide» (для продолжения работы)

> Хэндофф-документ для следующей сессии: что есть, как запускать, что осталось, где
> остановились. Дизайн-спека — `ARCHITECTURE.md`; выбор и удешевление модели —
> `MODEL_COMPARISON.md`; канонические промпты — `backend/prompts/*.txt`.

Обновлено: 2026-06-30. Папка: `D:\VS_Code\AI Guide`. Windows 11, PowerShell + Git Bash.
Git: по коммиту на фичу (см. `git log`).

---

## 0. Надёжность реального времени — фикс после прогулки 2026-06-25 (2026-06-26)

**Симптом на прогулке:** «бэкенд отваливался перманентно», паузы до 5+ минут, не бесшовно.
**Диагноз по логам прода** (`docker logs ai-guide`, вечер 25.06): контейнер **не падал**
(RestartCount=0, без OOM), **ноль ошибок LLM** — все вызовы deepseek успешны. Реальная
причина: **WS-соединение постоянно рвалось** (14 коннектов за 45 мин, по 30с–2мин; два окна
без связи — 15 мин и 3 мин = «5-минутные паузы»). Без keepalive мобильный NAT убивал idle-
сокет; каждое переподключение = **новая сессия с нуля** (uuid на сокет + delete при разрыве)
→ повторы и потеря контекста.

**Что сделано и задеплоено на прод (`178.83.121.62`, проверено сквозным `wss://` смоуком):**
- **WS-heartbeat** в обе стороны (сервер шлёт `{"type":"ping"}` каждые 20с; клиент — каждые
  15с) — `app/main.py` `run_heartbeat`, `mobile/lib/main.dart` `_heartbeat`. Держит сокет
  живым в паузах рассказа. Проверено: сервер реально шлёт ping на 20-й секунде idle-коннекта.
- **Resume сессии**: клиент шлёт стабильный `?sid=` (генерится раз за запуск), бэкенд берёт
  его как `session_id` и **больше не удаляет сессию при разрыве** (TTL/LRU чистят сами) —
  `_session_id_for`. Переподключение продолжает ту же экскурсию (проверено: 2-я сессия с тем
  же sid не повторяет интро района, а выдаёт новое место).
- **Утечка `_ip_conns`**: инкремент перенесён после accept и внутрь try → глючащий телефон
  больше не «забивает» лимит на IP и не блокирует сам себя.
- **STT off-loop**: `WhisperModel` грузится в треде под локом (`get_stt` теперь async) —
  первый голосовой вопрос больше не морозит весь event loop.
- **Дедлайны тика** («внятно в любой точке планеты»): LLM httpx-таймаут 45→22с; Overpass
  30→15с (+ query timeout 25→15), геокодер 20→12с; общий дедлайн discovery 20с в
  `on_position` — при тормозах гид продолжает рассказ о текущем месте, а не молчит.
- **Прод `.env`**: убраны reasoning-параметры (deepseek — не reasoning-модель), цены
  исправлены на deepseek (0.3/0.9), добавлен `USD_HARD_CAP=50` (бэкстоп; жёсткий месячный
  лимит всё ещё нужен в дашборде OpenRouter). Бэкап: `.env.bak.*` на сервере.

Тесты: бэкенд **66 passed, 1 skipped** (добавлены `test_ws_ping_is_ignored`,
`test_ws_resume_keeps_session_after_disconnect`); `flutter analyze`/`flutter test` зелёные.
Свежий debug-APK на прод: `mobile/build/app/outputs/flutter-apk/app-debug.apk`
(`--dart-define=WS_URL=wss://178.83.121.62.sslip.io/ws`).

**Деплой кода на сервер** (он НЕ git-репо, просто файлы): tar `backend/{app,prompts}` →
scp в `/root/aiguide/backend/` → распаковать (НЕ трогая `.env`) → `cd /root/aiguide/deploy
&& docker compose up -d --build backend`. SSH: `root@178.83.121.62`. Деплой одного файла:
`scp backend/app/.../X.py root@…:/root/aiguide/backend/app/.../` затем rebuild. Прод `.env`
лежит в `/root/aiguide/backend/.env` (НЕ в `deploy/`); бэкапы `.env.bak.*` рядом.
SSH без ключа из Git Bash: askpass-скрипт (`printf '%s\n' '<pass>'`) + `SSH_ASKPASS=… 
SSH_ASKPASS_REQUIRE=force DISPLAY=:0 ssh …` (sshpass нет; пароль с `%%`).

## 0a. Марафон фиксов 2026-06-26 (день 2) + находки прогулки. Версия зафиксирована: `caa2d46`

Продолжение после дня 1. Каждый шаг задеплоен на прод и проверён сквозным смоуком.

- **Голосовой барж-ин починен (две правки).** (1) Кнопка «спросить» на вебе ничего не делала:
  путь записи был мобильный (`getTemporaryDirectory` + `dart:io File` — кидают на вебе,
  исключение глоталось). Теперь захват PCM16 в память через `record.startStream` + ручной
  WAV-контейнер (`_wavFromPcm16`) — кроссплатформенно. (2) **«Стоп-и-слушай»**: новое WS-
  сообщение `listen on/off`; при нажатии микрофона клиент шлёт `listen on`, бэкенд отменяет
  текущий шаг и **держит продюсера** (`_step` молчит пока `listening`), чтобы гид не говорил
  поверх вопроса и его TTS не попадал в микрофон. Пустой/битый транскрипт → «Не расслышал —
  повтори», а не размытый ответ. Тесты: `test_ws_audio_empty_transcript_errors`,
  `test_ws_listen_pauses_then_question_resumes`.
- **Overpass — единая точка отказа → мульти-мирор с фейловером.** Симптом «вообще всё пропало,
  даже не начинает говорить» = сконфигурированное зеркало `maps.mail.ru` тормозило (~13–15с,
  за таймаутом). Один endpoint обслуживает И discovery, И геокодер → отвал обоих = полная
  тишина. `fetch_overpass_elements()` (providers.py) теперь перебирает список зеркал, первый
  JSON-200 побеждает, короткий таймаут на зеркало. Рабочие с прод-хоста (HK): **`z.overpass-
  api.de`** (реальные данные, ~1–5с — сделан primary в `.env`), `maps.mail.ru` (медленный,
  fallback). `overpass.osm.ch` БЫСТРЫЙ но **только Швейцария** (пусто для остального мира) —
  НЕ использовать. См. memory `overpass-mirrors`.
- **Расширен список объектов** (`providers.py` `_SELECTORS` + `categories.py`): площади,
  пешеходные улицы, университеты/библиотеки/рынки/ратуши/монастыри, кладбища, стадионы,
  акведуки/городские ворота, холмы/пещеры/арки, и «знаковые здания» по `building=` (собор,
  дворец, замок, вокзал…) — мапятся на свою настоящую категорию, не «building».
- **Discovery в пригороде** (симптом в Долгопрудном: про район рассказывает, к объектам не
  переходит, бесконечно расширяет радиус). Замерено: ближайшие объекты 166–300м — за
  `weave_radius_m`=120, и `discover_adaptive` шёл 80→200→500 (3 тяжёлых запроса, 17–32с) >
  дедлайна 14с. Фиксы: `weave_radius_m` 120→**300**, `default_radius_m` 80→**300** (один
  запрос покрывает и центр, и пригород; прод `.env` тоже 300); `discover_adaptive` теперь
  **макс 2 запроса** (узкий → сразу один широкий до max), близость — ранжированием, не
  перезапросом; дедлайн 14→20с, таймаут зеркала 11→8с; в оркестраторе **fallback**: если в
  weave-радиусе пусто, но discovery что-то нашёл — озвучиваем ближайший, а не молчим.

**Прогулка 2026-06-26 (~74 мин, 11:41–12:55, sid `i6hwvd…`, 6 переподключений — все
продолжили ту же сессию). Фидбэк лида/тестера и что показали логи:**
- *Положительно:* искало быстро, переключалось быстро, рассказ хороший. 407 narrator, 13
  landmark, 14 companion (вопросы), 6 scorer; ~$0.55; 0 падений; связи 11/11 (без шторма).
- *«Почти не переключалась по иерархии, паузы хочется заполнять связками о городе/районе»* —
  scorer всего 6 (набор кандидатов редко менялся принципиально) → больше элаборации, чем
  switch. Area-монолог имеет потолок (`_MAX_ELABORATE`, лимит area-бит) → после исчерпания
  тишина. Рычаг контента: поднять бюджет связующих рассказов о районе.
- *«Ответы не учитывали геолокацию, со временем начали»* — геокодер move-gated: адрес (улица)
  резолвится после прохода; ранние вопросы → `st.address` пуст. Фикс на будущее: резолвить
  адрес сразу на первом фиксе и явно передавать координаты в companion.
- *«Некоторые объекты не находила, но на прямой вопрос отвечала верно»* — discovery видит
  только именованные OSM-объекты в радиусе; companion отвечает из знаний модели (не из OSM).
  Это пробел покрытия OSM, не баг.
- *«В браузере распознавание не работает, в приложении ок»* — STT у нас на сервере (Whisper),
  не в Chrome. В приложении захват PCM→WAV корректный; в браузере веб-`record` на Chrome,
  видимо, отдаёт пустой звук → «не расслышал». Веб-STT — открытый пункт.

**ГЛАВНАЯ НАХОДКА — баг #1 на следующую версию (сейчас НЕ трогаем, версия заморожена):
зомби-продюсер.** В логах **1261** `producer step failed: Unexpected ASGI message
'websocket.send' after close`. Механизм: при дисконнекте `ws` finally вызывает
`producer.cancel()`, НО `run_producer` в `except asyncio.CancelledError: if self.barging:`
**проглатывает отмену** (трактует как преемпт барж-ина) и `continue` — продюсер становится
зомби, вечно крутится, шлёт в закрытый сокет. Хуже: зомби каждый тик трогает общий
`SessionState` (тот же sid) → **TTL не вытесняет** (immortal) и он **гоняет состояние
наперегонки с живым продюсером** (вероятный вклад в «рваную иерархию/паузы») + жжёт narrator.
Проверено: зомби с прогулки крутился спустя 2.5ч (~1 ошибка/сек, ~10% CPU). Память ОК (270МБ
из 3.8ГБ). **Фикс:** не глотать отмену, когда это настоящий shutdown (различать барж-ин-
преемпт и cancel из finally — напр. флаг `self.closing`, или barging-ветка только если
`self.resume` ещё ждут). Временная мера без правок кода: `docker compose restart backend`
чистит зомби.

**Разбор полётов — почему в МСК «работало», а у тестера «всё упало».** Код был один и тот же —
**разные сетевые условия**. Прод-сервер физически в **Гонконге** (CloudBlast LLC, `Asia/Hong_Kong`;
старое утверждение про «регион РФ» в §5 неточно — на цену/блок не влияет, deepseek доступен).
Прошлые «успешные» тесты шли либо через **эмулятор + `adb reverse` / LAN** (это вообще **локальный**
бэкенд, нулевая сеть, сокет не рвётся в принципе), либо со **стационарного WiFi** короткими
сессиями (NAT не реапит, хендоверов нет). Тестер же впервые прошёл **реальную 45-мин прогулку на
мобильном интернете до сервера в Гонконге**: высокий RTT, хендоверы между вышками, CGNAT,
дыры покрытия — без keepalive/resume это и дало шторм переподключений и «5-минутные паузы». То есть
баг проявляется **только в полевых условиях**, которые прежние тесты не воспроизводили. Точный IP
тестера по логам **не восстановить** (access-логи Caddy выключены, бэкенд видит только прокси
`172.18.0.3`); поведенческая подпись (частые обрывы + два провала связи) — это телефон в движении на
сотовой сети. На будущее: включить `log` в `deploy/Caddyfile`, чтобы фиксировать IP/гео клиентов.

**Веб-клиент теперь = Flutter web (2026-06-26).** Раньше веб был рукописным HTML наспех.
Теперь Caddy отдаёт **сборку Flutter под web** (`flutter build web --release
--dart-define=WS_URL=wss://178.83.121.62.sslip.io/ws`) статикой из `deploy/web/`, а на бэкенд
проксирует только `/ws /health /ready /stats`. UI/логика **идентичны** мобильному приложению
(heartbeat/sid/статус — из коробки). Важно: в `Caddyfile` маршруты заданы блоками `handle`
(иначе `try_files {path} /index.html` перехватывает `/health` раньше прокси). Caddy-конфиг
перечитывается **без пересоздания** контейнера: `docker exec ai-guide-caddy caddy reload
--config /etc/caddy/Caddyfile`. Рукописные `backend/web/*.html` больше не отдаются (legacy).

**Caddy access-логи включены** (`log { format json }` → `docker logs ai-guide-caddy`): теперь
видно реальный `client_ip`/URI каждого запроса (бэкенд за прокси видит только внутренний IP).

**Статус при ошибке апстрима виден в UI:** пилюля статуса теперь показывает `error/recovery`
→ оранжевое «источник недоступен», `offline` → красное (8 языков, `chipError`/`chipOffline`).
Бэкенд и так слал `{"type":"state","state":"error"}` — клиент просто перестал прятать это в
«ready».

**Осталось (не блокеры MVP):** `WS_TOKEN` + перевыпуск ключа OpenRouter + жёсткий лимит в
дашборде (эндпоинт пока открыт); коммит изменений в git (сделаю следующим шагом).

## 0b. Безопасность MVP-тир + стабилизация связи (2026-06-30) — `3fa7bba`

Проведён аудит безопасности серверной и клиентской части (WS/auth, app-логика/SSRF/секреты,
клиент+деплой). `/ws` сейчас **публичный/открытый** в проде (`ws_token=""`) — это принятая
MVP-позиция; реальный бэкстоп — **спенд-капы**, а не shared-токен (токен, зашитый в публичную
веб-страницу и APK, — не настоящая аутентификация). Полный список находок — в роадмапе ниже.

**Сделано и задеплоено:**
- **Устойчивый WS-приём** (`app/main.py` `ws()`): `receive_text` + проверка размера кадра
  (`MAX_WS_FRAME_CHARS`) **до** парсинга; кривой JSON / не-объект / невалидное типизированное
  сообщение → типизированный `{"type":"error"}` + `continue` (раньше один битый `position`
  ронял сокет). Только `WebSocketDisconnect` завершает цикл. Диспатч вынесен в `_dispatch()`.
- **Спенд-нудж** (`C1`): WARNING при старте, если платный бэкенд работает с `USD_HARD_CAP=0`.
- **Безопасный sid**: клиент минтит 32-символьный `Random.secure()`; сервер `_SID_RE` → `{16,64}`.
- **`.gitignore`**: добавлены `*.apk/*.aab/*.ipa/*.keystore/*.jks/*.p12/key.properties`.
- **Стабилизация связи (клиент, `main.dart`):** liveness-**watchdog** — форс-реконнект после
  >40с тишины во входящем потоке (ловит half-open сокет в метро/лифте, когда `onDone` не
  срабатывает); **повтор последней позиции на реконнекте** (тур продолжается, не ждёт GPS-тик);
  **джиттер в backoff** (нет реконнект-шторма после рестарта сервера).

Тесты: бэкенд **101 passed, 1 skipped** (+`test_ws_survives_malformed_json_and_invalid_message`,
`test_ws_caps_oversized_frame_before_parsing`); `flutter analyze`/`flutter test` зелёные.
Живой прод-смоук (`wss://`): битый кадр → `error`, **сокет выживает**, валидная позиция → `state`.

**Открытые действия пользователя (я не делаю — не трогаю прод `.env`/дашборд):** (1) **перевыпустить
ключ OpenRouter** — реальный `sk-or-...` лежит в `backend/.env` (gitignored) и на прод-хосте,
засветился в аудите; (2) подтвердить `USD_HARD_CAP` + месячный лимит в дашборде OpenRouter
(при старте прода предупреждения не было → кап, вероятно, уже стоит).

## 0c. Roadmap дальнейшей реализации (порядок умышленный — «чтобы не забыть»)

Решение по последовательности (2026-06-30): **аккаунты/авторизацию/историю прогулок делаем НЕ
сейчас, а на пред-проде, последним milestone.** Обоснование: (а) аккаунты не улучшают то, что
сейчас валидируется (качество агента, латентность, i18n, стабильность сокета) — ядро продукта
анонимное по дизайну («открыл и пошёл»); (б) **долга миграции нет** — прогулки сейчас вообще не
персистятся, поэтому позже это NET-NEW, а не переделка → откладывать НЕ дороже; (в) auth — это
ровно та security-поверхность, которую аудит велел отложить (H2), и тащить её через MVP-churn
дорого; (г) реальная стоимость — недели + постоянная нагрузка (БД, бэкапы, privacy/GDPR).

**Этап A — СЕЙЧАС: реальные прогулки.** Догнать качество агента и стабильность связи на живых
тестерах (см. §3). Это нельзя проверить иначе. Аккаунты — gate на *запуск*, а не на тестирование.

**Этап B — пред-прод, milestone 1: отложенный security-тир** (из аудита 0b; каждый пункт сейчас
умышленно НЕ сделан, т.к. мешал бы MVP `ws://`-тестам по LAN/эмулятору или нужен только против
недоверенной публики):
- `H2` — настоящая per-user аутентификация (приходит вместе с Этапом C).
- `H3` — Caddy security-заголовки (HSTS/CSP/X-Content-Type-Options).
- `H5` — корректное доверие `X-Forwarded-For` (сейчас спуфится).
- `M2` — constant-time сравнение токенов; `M4` — лимиты размера ответов httpx +
  `follow_redirects=False` (SSRF-через-редирект); `M5` — границы координат во входных position;
  `M6` — `/stats` только внутрь; `M7` — выключить Android `usesCleartextTraffic` / iOS
  `NSAllowsArbitraryLoads` (**держим ВКЛ для тестов**); `M8` — per-session rate-limit сообщений.

**Этап C — пред-прод, milestone 2 (последний): аккаунты + история прогулок.** Делать, когда
схема агента устоялась, чтобы БД ставить один раз под устоявшуюся модель. Объём:
- **Хранилище**: durable БД (Postgres или SQLite) на прод-хосте — бэкапы, миграции. Сейчас
  `SessionState` в памяти (Redis опционально) — это новая инфра-зависимость.
- **Провайдеры авторизации**: вход через **Google**, **Apple** (на iOS), **создание аккаунта по
  почте**. Email: хэш паролей, верификация почты, сброс пароля, rate-limit (всё security-critical).
  Google: OAuth2, отдельные client ID под Android/web/iOS, верификация токена на бэке. Apple:
  Sign in with Apple **обязателен в App Store, если есть другие соц-логины**; требует Apple
  Developer Program (~$99/год), Service ID, ротацию client-secret JWT по ключу; только iOS.
- **Сессии/токены**: JWT или серверные сессии; на клиенте — `flutter_secure_storage`; refresh.
- **Модель данных**: таблицы `users`, `walks` (маршрут, озвученные места, таймстемпы),
  привязка WS-сессии к `user_id`. Форвард-совместимость уже есть: стабильный `sid` станет
  «гостевой» сессией, залогиненная просто добавит `user_id` → WS-контракт менять не придётся.
- **Анонимный режим остаётся**: без аккаунта пользоваться можно, но **без сохранения истории**.
  Нужна миграция «гость → аккаунт» (привязать накопленную гостевую прогулку при логине).
- **Client UI**: экран логина, экран аккаунта, лента истории (список + детали прогулки).
- **Приватность/legal**: история маршрутов = геолокационные PII → privacy policy, согласие,
  удаление данных (GDPR). Возникает ровно в момент, когда начинаем хранить чужие маршруты.

## 0d. Фоновый режим — гулять с заблокированным экраном (2026-06-30)

**Запрос:** выключить экран, положить телефон в карман, идти с наушником — гид продолжает
рассказ. **Решение (без второго изолята):** держим процесс живым **foreground-сервисом
геолокации**, который уже встроен в `geolocator`. Пока сервис активен, главный изолят не
вытесняется — GPS-поток, WebSocket (heartbeat/watchdog) и `flutter_tts` продолжают работать с
локом, потому что таймеры/стримы/сеть живут на event-loop (а не на vsync-кадрах, которые
останавливаются вне видимости). Ядро агента и серверная часть **не тронуты**.

Что сделано (клиент):
- **Android-манифест**: `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_LOCATION`,
  `ACCESS_BACKGROUND_LOCATION` (опц.), `POST_NOTIFICATIONS`, `WAKE_LOCK`. Сам сервис
  (`GeolocatorLocationService`, `foregroundServiceType="location"`) объявляет плагин — свой не нужен.
- **`_startGps`** (`main.dart`): вместо базового `LocationSettings` — платформенные. Android:
  `AndroidSettings` с `foregroundNotificationConfig` (постоянное уведомление + `enableWakeLock`).
  iOS: `AppleSettings(allowBackgroundLocationUpdates:true, pauseLocationUpdatesAutomatically:false,
  activityType: fitness)`. На `Stop` поток отменяется → сервис и уведомление исчезают.
- **`_initTts`**: на iOS ставим audio-session `playback` (+ `duckOthers`, `allowBluetoothA2DP`,
  `allowAirPlay`, mode `spokenAudio`) — озвучка играет в наушник при локе и приглушает музыку.
- **iOS `Info.plist`**: `UIBackgroundModes = [location, audio]` + `NSLocationAlwaysAndWhenInUse…`.
- **Локализация уведомления**: `bgNotifTitle`/`bgNotifText` в 8 ARB (`flutter gen-l10n`
  перегенерил `lib/l10n/app_localizations.dart` — он в гите).

Проверки: `flutter analyze` чисто, `flutter test` зелёный. (Полевой тест на реальном телефоне с
заблокированным экраном — за пользователем.)

Гочи/ограничения:
- **Постоянное уведомление обязательно** на Android для FG-сервиса — убрать нельзя, это плата
  за работу в фоне. Исчезает на `Stop`.
- **Foreground-service-while-in-use**: сервис стартует, пока приложение на переднем плане (юзер
  жмёт «идти»), поэтому хватает разрешения **«при использовании»** — отдельный запрос «Разрешить
  всегда» (`ACCESS_BACKGROUND_LOCATION`) для MVP не делаем (это редирект в настройки на Android
  11+). Разрешение объявлено — можно выдать вручную для пущей живучести на «убийцах» фона.
- **Агрессивные OEM** (Xiaomi/Huawei/Samsung) могут убивать даже FG-сервис без «не оптимизировать
  батарею» — на полевом тесте, если фон рвётся, отключить battery-optimization для приложения.
- **POST_NOTIFICATIONS (Android 13+)**: если уведомления запрещены, баннер не покажется, но
  **сервис всё равно работает**. Рантайм-запрос не добавлял (без `permission_handler`).
- **Web фон не умеет** (нет FG-сервиса; вкладка в локе засыпает) — фон только в APK/iOS, веб
  остаётся для быстрых проверок.

---

## 1. Что это и как устроено

Автономный аудиогид реального времени: GPS+направление → находим места (OSM) → оцениваем
значимость → обогащаем фактами → **озвучиваем живой рассказ**; можно **прервать голосом** и
порулить экскурсией. Один оркестратор-«мозг» + 3 stateless LLM-роли (**Scorer**→JSON,
**Narrator**→текст, **Companion**→чат, плюс **Landmark** — премиум-нарратор для значимости
LANDMARK) через общее состояние сессии. Службы: Geo (OSM/Overpass), Enrichment
(Wikipedia/Wikidata + платный веб-поиск, кэш), STT, TTS-интерфейс, State (in-memory/Redis).
Клиент-сервер по WebSocket; клиент — Flutter (карта на весь экран, тёмная тема).

---

## 2. Что РАБОТАЕТ (проверено end-to-end)

Бэкенд на **облачном Gemini 3.5 Flash** (OpenRouter) ИЛИ локальном **qwen** (LM Studio) —
переключается одним блоком в `.env`. Flutter-приложение **собирается в APK и работает на
Android-эмуляторе** `guide_emu`.

| Область | Статус | Суть |
|---|---|---|
| Бэкенд-ядро | ✅ | FastAPI + asyncio + WS; FSM, память сессии, эвристический гейт, control_patch, OFFLINE/ERROR/RECOVERY |
| Geo | ✅ | **Реальный Overpass** (зеркало mail.ru), ранжирование (дистанция/тип/конус), адаптивный радиус, дедуп. Селекторы: культура+**природа/вода** (реки, озёра, водохранилища, леса, сады) + структуры (мосты, башни). Линейные объекты привязываются к **ближайшей** к юзеру точке (`out geom`) |
| Промпты + пайплайн | ✅ | CORE+роли; Scorer/Narrator/Companion/Landmark; провайдер-агностичный `LLMClient` |
| **LLM: Gemini 3.5 Flash** | ✅ | OpenRouter на все роли; локальный qwen — фолбэк. Метр токенов/стоимости |
| **Обогащение фактами** | ✅ | **Wikipedia/Wikidata (бесплатно)** → платный веб-поиск (OpenRouter) только для мест **без** вики-тега (`CompositeEnricher`). Off the hot-path: топ-K с таймаутом 9с, кэш память+диск, дизамбигуация по координатам. ~$0.008/место с вики vs ~$0.038 поиском. `mock` для тестов |
| **Поведение рассказа** | ✅ | Бесшовный монолог **без вопросов** к слушателю; факты — приоритет (история/детали), у рядовых мест короткое упоминание без выдумок; **«зацепка»** — когда рядом пусто, гид продолжает про текущее место (до 6 догонов) |
| **STT (голосовой barge-in)** | ✅ | Реальный `faster-whisper` (CPU/int8). `MockSTT` для тестов |
| TTS | ✅ (на клиенте) | On-device `flutter_tts`, голос по выбранному языку. Серверный TTS = NullTTS (Piper не делали) |
| **Flutter — карта на весь экран, тёмная** | ✅ | CARTO dark тайлы; плавающие пилюли (бренд/язык/звук/настройки); стеклянная нижняя карточка (статус-пульс, место+рассказ, одна главная кнопка connect+walk/stop) |
| — пины мест | ✅ | Текущий жёлтый / пройденные серые, **кликабельные** → шторка с названием и историей места |
| — камера | ✅ | **Плавная** центровка (easeInOut); кнопка-компас (ориентация на север); курсор держится **над карточкой** (видим) |
| — озвучка | ✅ | Барджин глушит; **не режет фразу на полуслове** (новый рассказ ждёт окончания, играет самый свежий) |
| — голосовой/текстовый вопрос | ✅ | Микрофон (`record`→WAV→STT) или клавиатура → ответ Companion (озвучивается) |
| — настройки/история | ✅ | Dev-обвязка (WS URL, тумблер симуляции) в шторке; лента сообщений — swipe-up «История»; авто-reconnect |
| **Мультиязычность** | ✅ | 8 языков (en/ru/es/fr/de/it/pt/zh): narration+TTS+STT по сессии; переключатель + автодетект системного (фолбэк EN); полная локализация UI (gen-l10n). Язык по WS (`{type:"language"}`) на коннекте/смене |
| **Демо-маршрут** | ✅ | Симуляция идёт по реальному маршруту (Волгоградский пр-т → Павелецкая) на скорости пешехода (~7 км/ч), как реальный GPS |

**Демо сейчас:**
- Эмулятор `guide_emu` + APK — мобильный клиент идёт по реальному маршруту (Волгоградский →
  Павелецкая) на скорости пешехода и **рассказывает с фактами** (карта, пины, озвучка, микрофон).
- `python -m sim.e2e_regions` — прогон по 12 регионам РФ/заграницы (реальный Overpass + агент);
  `OVERPASS_URL=...mail.ru...`, `E2E_ONLY=msk-red-square,...` — подмножество. См. `E2E_REGIONS.md`.
- `python -m sim.run_orchestrator --llm openai` — полный агент на фикстурах.
- `python -m sim.eval_live --n 5` — метрики качества + лог токенов/стоимости.
- Docker: `cd backend && docker compose up -d --build` — деплой на LAN-сервер.
- Smoke: `sim.smoke_openrouter` (LLM), `sim.smoke_stt <wav>` (STT), `sim.smoke_cache` (кэш).

---

## 3. Что ОСТАЛОСЬ

- **Связь на прогулке (главное для прода).** Гуляющему телефону нужен бэкенд, доступный
  **по дороге**, а не только в домашнем WiFi — иначе на улице связь рвётся (проверено: на
  реальной прогулке телефон выходил из зоны WiFi → гид замолкал). Варианты: туннель
  (cloudflared/ngrok) с ПК, приватный VPN (Tailscale) или облачный сервер с публичным IP.
  Air-gapped homelab не подходит — бэкенду нужен интернет (LLM + Overpass + факты).
- **Тест на реальном телефоне** — реальный GPS, голоса TTS и микрофон (у эмулятора GPS
  фейковый, голоса не всех языков; эмулятор в этой машине ещё и нестабилен по графике).
- **Резолв адреса (город) для обогащения** — в запрос факта сейчас идут координаты; с
  реальным городом (адрес-резолв 1.2) дизамбигуация ещё надёжнее.
- **Локализация системных промптов** — `{language}` управляет языком вывода; текст правил в
  `core.txt`/ролях остаётся русским. Опционально: per-language файлы промптов.
- **Серверный TTS (Piper)** — опционально; сейчас озвучивает клиент.
- **Продакшн-карта** — публичные CARTO/OSM-тайлы не для нагрузки; свой тайл-сервер/провайдер.
- **Безопасность/бюджет:** перевыпустить ключ OpenRouter (светился в чате) и поставить
  жёсткий лимит $25/мес в дашборде OpenRouter (в коде только мягкое предупреждение).
- **GPU STT** — доустановить `nvidia-cublas-cu12`/`nvidia-cudnn-cu12`, тогда `WHISPER_DEVICE=cuda`.

---

## 4. Как запускать

**Бэкенд** (из `D:\VS_Code\AI Guide\backend`):
```powershell
$env:PYTHONIOENCODING="utf-8"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```
`--host 0.0.0.0` нужен, чтобы достучаться с эмулятора/телефона. Health: `http://localhost:8000/health`.

**Flutter (эмулятор):**
```powershell
$env:Path = "C:\FlutterSDK\flutter\bin;" + $env:Path
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"   # для sdkmanager/avdmanager/emulator
flutter emulators --launch guide_emu
cd "D:\VS_Code\AI Guide\mobile"; flutter build apk --debug
# установка/запуск:
$env:Path = "C:\Users\efimr\AppData\Local\Android\Sdk\platform-tools;" + $env:Path
adb -s emulator-5554 install -r build\app\outputs\flutter-apk\app-debug.apk
adb -s emulator-5554 reverse tcp:8000 tcp:8000          # эмулятор: localhost:8000 -> хост
adb -s emulator-5554 shell pm grant com.example.ai_audio_guide android.permission.RECORD_AUDIO
adb -s emulator-5554 shell monkey -p com.example.ai_audio_guide -c android.intent.category.LAUNCHER 1
```
В приложении: дефолтный URL `ws://localhost:8000/ws` работает как есть (через `adb reverse`).
На реальном телефоне — вписать `ws://<LAN-IP-ПК>:8000/ws`.

---

## 5. Окружение (факты)

- **Python 3.14**, venv в `backend\.venv`. Зависимости поставлены (FastAPI, httpx, pydantic,
  faster-whisper, ctranslate2, websockets).
- **Flutter 3.44.2** в `C:\FlutterSDK\flutter\bin` (Dart 3.12.2). PATH прописан, но в свежих
  shell-ах префиксь на всякий случай.
- **Android SDK** в `C:\Users\efimr\AppData\Local\Android\Sdk`; лицензии приняты; системный
  образ `system-images;android-35;google_apis;x86_64`; AVD **`guide_emu`** (Pixel 6).
- **JBR JDK 21** в `C:\Program Files\Android\Android Studio\jbr` — задавать как `JAVA_HOME`
  для `sdkmanager`/`avdmanager`/эмулятора (иначе «Java 17+ required»).
- **LM Studio** (если используем qwen): `http://localhost:1234/v1`, модель `qwen/qwen3.5-9b`,
  RTX 3060 12GB.
- **OpenRouter**: ключ в `backend/.env` (gitignored). Модель в деве — `google/gemini-3.5-flash`,
  **в проде — `deepseek/deepseek-chat`** (см. ниже про региональный блок).

> ⚠️ **Региональный блок (прод-хост, 2026-06-25).** OpenRouter геоблокирует OpenAI / Anthropic /
> Google (Gemini) из региона нашего сервера (РФ): любой вызов → HTTP 403 *"This model is not
> available in your region."* — гид подключался, но молчал. Фикс: на проде `OPENAI_MODEL=deepseek/deepseek-chat`
> (доступен из региона, тянет строгий `json_schema` Scorer'а и мультиязычный рассказ; reasoning-кап
> не нужен — V3 не reasoning-модель). Проверено сквозным WS-смоуком: рассказ по Красной площади
> приходит. В деве Gemini остаётся.

---

## 6. Конфиг `.env` (gitignored, `backend/.env`)

Облачный Gemini (текущий) — ключевые строки:
```
AGENT_BACKEND=openai
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_API_KEY=sk-or-...                 # вне git; перевыпустить
OPENAI_MODEL=google/gemini-3.5-flash
OPENAI_REASONING_EFFORT=low
OPENAI_REASONING_MAX_TOKENS=64           # кап reasoning для Narrator/Landmark/Enricher
OPENAI_PROMPT_CACHE=true                 # + точный учёт стоимости (usage.include)
OPENAI_PRICE_IN_PER_MTOK=1.5
OPENAI_PRICE_OUT_PER_MTOK=9.0
USD_SESSION_BUDGET=25                    # мягкое предупреждение, не жёсткий кап
STT_BACKEND=faster_whisper
WHISPER_MODEL_SIZE=small
WHISPER_DEVICE=cpu                       # cuda нужен cublas/cudnn на PATH
WHISPER_COMPUTE_TYPE=int8
DEFAULT_LANGUAGE=ru
GEO_SOURCE=overpass                      # ОБЯЗАТЕЛЬНО для реальной прогулки (fixture = только Красная пл.)
OVERPASS_URL=https://maps.mail.ru/osm/tools/overpass/api/interpreter   # публичный overpass-api.de часто блокируется
ENRICHMENT_SOURCE=websearch              # mock => фикстуры (тесты); websearch => Wikipedia + платный поиск
WEB_SEARCH_MAX_RESULTS=2                 # web-результатов на платный поиск (3 дороже ~$0.13, 2 ~ $0.032)
WEB_SEARCH_MAX_TOKENS=400
ENRICH_TOP_K=2                           # сколько топ-кандидатов обогащать за тик (префетч наперёд)
ENRICH_TIMEOUT_S=9.0                     # веб-поиск занимает ~5-7с; меньше — фактов не будет, только названия
ENRICH_MIN_WEIGHT=0.0                    # вики бесплатна всегда; гейтит только ПЛАТНЫЙ fallback (0 = искать всё)
ENRICH_CACHE_PATH=.enrich_cache.json     # кэш фактов на диск (gitignored); "" => только память
```
Откат на локальный qwen: заменить `OPENAI_BASE_URL/KEY/MODEL` на LM Studio (закомментированы
в `.env`), `OPENAI_REASONING_*`/`OPENAI_PROMPT_CACHE` можно убрать (LM Studio их не понимает).

---

## 7. Гочи (на чём уже спотыкались)

- **Сборка Kotlin падает «different roots»** — pub-кэш на `C:`, проект на `D:`. Лечение уже
  в `mobile/android/gradle.properties`: `kotlin.incremental=false`.
- **`flutter_compass` выкинули** — не обновлялся, ломал AGP 8 (нет `namespace`). Heading
  берём из курса GPS (`position.heading`), `gaze_confidence=low` — это и есть фолбэк
  «компас в кармане» из дизайна.
- **`record` запинён на `^7.0.0`** — 5.x разрешался в несовместимый федеративный набор
  (record_linux без `startStream`).
- **Gemini 3.x: ризонинг отключить нельзя** («Reasoning is mandatory»). `effort=low` всё
  равно тратит ~380 дорогих выходных токенов → капаем `reasoning.max_tokens` для
  **Narrator/Landmark/Enricher** (у Landmark без капа Gemini сливал «план рассуждений» в текст).
- **Веб-поиск фактов занимает ~5-7с** — таймаут обогащения должен быть **≥9с**, иначе запрос
  отменяется до ответа и нарратор остаётся без фактов («тут находится X»). Один поиск ~$0.032
  (≈85% цены места) → берём факты из **Википедии бесплатно**, платим только за не-вики хвост.
- **Wikimedia требует осмысленный User-Agent** — «голый» UA даёт 403. У `WikiEnricher` стоит
  корректный с контактом + `follow_redirects=True`.
- **Линейные OSM-объекты** (канал/река): `out center` отдаёт середину линии (за километры) →
  тянем `out geom` и привязываем к ближайшей точке. Объекты, названные только на route-релации
  (напр. «Канал имени Москвы» — 128 км), намеренно пропускаем (огромный payload).
- **Prompt-caching у Gemini/OpenRouter слабый** (`cached=0` в логах) — и даже работая, кэшировал
  бы только дешёвые Scorer/Narrator, а не платный поиск. Реальную цену не снижает.
- **Эвристический гейт** уже снижает вызовы Scorer, но веб-поиск не трогает (кэш и так ищет
  место один раз). Главный рычаг цены — бесплатные вики-факты, а не гейт/prompt-caching.
- **STT на GPU** требует `cublas64_12.dll` (CUDA runtime) — пока CPU/int8 (~2с на реплику).
- **Эмулятор `guide_emu` нестабилен под нагрузкой** — за сессию уходил в `offline`, ANR и
  GPU-глитч (битый синий кадр в `screencap`). Не баг приложения (в logcat нет исключений Flutter).
  Лечится перезапуском эмулятора; на реальном телефоне рендер нормальный.
- **OSM-тайлы**: публичные CARTO/OSM не для прод-нагрузки.
- **Кириллица в консоли**: `$env:PYTHONIOENCODING="utf-8"` + `sys.stdout.reconfigure(...)`.
- **Драйв эмулятора через adb**: после `monkey`-запуска нужно ~13с на холодный старт — иначе
  тапы уходят в splash.

---

## 8. Где остановились → следующий шаг

**Этап 7 практически закрыт — по сути MVP готов.** За сессию сделали (по коммиту на фичу):
- **Мультиязычность** — 8 языков, переключатель + автодетект, полная локализация UI (gen-l10n),
  язык по WS. Код: `services/agent/languages.py`, `main.dart` (`kLangs`, `_changeLanguage`), `l10n/*.arb`.
- **Обогащение фактами** — прошли путь WebSearch → выборочное (откатили) → **Wikipedia-first
  composite**: вики-факты бесплатно, платный поиск только для не-вики (`CompositeEnricher`,
  `WikiEnricher`). Таймаут поднят до 9с (иначе фактов нет). Проверено: «Улица Талалихина» с
  полной историей при **0 платных поисков** (вики/кэш). Цена ~$0.008/место с вики vs ~$0.038.
- **Geo** — реальный Overpass + природа/вода в селекторах + ближайшая точка для линейных объектов.
- **Поведение** — бесшовный монолог без вопросов, факты-приоритет, «зацепка» (до 6 догонов),
  клиент не режет фразу на полуслове.
- **UI** — карта на весь экран (тёмная CARTO), стеклянная карточка, кликабельные пины,
  компас, плавная центровка, курсор над карточкой. Демо-маршрут на скорости пешехода.
- **Docker-деплой** на LAN-сервер (`Dockerfile`/`compose`/`.env.example`-прод-шаблон).

Тесты: бэкенд офлайн-набор зелёный (47 passed; live-тесты требуют сети), `flutter analyze`
чисто, `flutter test` зелёный, APK собран.

**Рекомендуемый следующий шаг:** решить **связь на прогулке** (туннель/VPN/облако — без этого
телефон теряет бэкенд за пределами WiFi) и протестировать на реальном телефоне. Перед демо
наружу — перевыпустить ключ OpenRouter и поставить лимит в дашборде.
